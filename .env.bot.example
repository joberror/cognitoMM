# MovieBot Configuration - Bot Session
# Copy this file to .env and fill in your values

# ==========================================
# REQUIRED CONFIGURATION
# ==========================================

# Telegram API credentials (get from https://my.telegram.org)
API_ID=your_api_id
API_HASH=your_api_hash

# Bot Token (get from @BotFather)
BOT_TOKEN=your_bot_token

# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGO_DB=moviebot

# Admin User IDs (comma-separated, no spaces)
# Example: ADMINS=123456789,987654321
ADMINS=123456789,987654321

# ==========================================
# OPTIONAL CONFIGURATION
# ==========================================

# Bot ID (optional - will be auto-detected if not provided)
# BOT_ID=0

# Log Channel ID for admin notifications and logging
# Create a private channel, add your bot as admin, and use the channel ID
# Example: LOG_CHANNEL=-1001234567890
LOG_CHANNEL=-1001234567890

# Search Configuration
# Fuzzy search threshold (0-100, higher = stricter matching)
FUZZY_THRESHOLD=68

# Auto-indexing (automatically index new messages in monitored channels)
# Options: True, False, 1, 0, yes, no
AUTO_INDEXING=True

# TMDb API Key (Required for /request feature with auto-search)
# Get your free API key from https://www.themoviedb.org/settings/api
# Leave empty to disable TMDb integration
TMDB_API=your_tmdb_api_key_here

# Broadcast Configuration
# Rate limit for broadcast messages (messages per second)
# Default: 25 (conservative, Telegram limit is ~30/s)
BROADCAST_RATE_LIMIT=25

# Progress update interval (update admin every N users)
# Default: 100
BROADCAST_PROGRESS_INTERVAL=100

# Test mode for broadcast (only send to test users)
# Options: True, False, 1, 0, yes, no
# Default: False
BROADCAST_TEST_MODE=False

# Test user IDs for broadcast testing (comma-separated, no spaces)
# Only used when BROADCAST_TEST_MODE=True
# Example: BROADCAST_TEST_USERS=123456789,987654321
BROADCAST_TEST_USERS=

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================
# 1. Create a bot with @BotFather on Telegram
# 2. Get the bot token and put it in BOT_TOKEN above
# 3. Get your API_ID and API_HASH from https://my.telegram.org
# 4. Set up MongoDB Atlas (free tier available) and get your connection URI
# 5. Add your Telegram user ID to ADMINS (get it from @userinfobot)
# 6. (Optional) Create a log channel and add its ID to LOG_CHANNEL
# 7. (Optional) Get TMDb API key for the request feature
# 8. (Optional) Configure broadcast settings for admin messaging
# 9. Add the bot as admin to channels you want to monitor
# 10. Use /add_channel command to add channels to monitoring
# 11. Bot must be admin in channels for file forwarding to work

# ==========================================
# NOTES
# ==========================================
# - This is a BOT SESSION configuration (uses BOT_TOKEN)
# - The bot uses Hydrogram library for better channel access
# - All admin commands require your user ID to be in ADMINS list
# - Log channel is useful for tracking bot activities and errors
# - TMDb integration enhances the /request feature with movie/series search
# - Broadcast feature allows admins to send messages to all eligible users
# - Use BROADCAST_TEST_MODE=True to test broadcasts safely with test users
