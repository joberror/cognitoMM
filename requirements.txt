pyroblack>=3.0.3  # Telegram MTProto framework (installs the `pyrogram` package); successor to hydrogram
pymongo
python-dotenv>=1.0.0
motor>=3.1.1
fuzzywuzzy>=0.18.0
python-levenshtein>=0.20.9  # optional but speeds up fuzzywuzzy
tgcrypto>=1.2.5  # for better performance
uvloop
requests
aiohttp
aiofiles
Pillow
Flask
waitress  # production WSGI server for the health-check webapp

# Test dependencies
# pytest-asyncio >= 1.4.0 guarantees asyncio_default_test_loop_scope (used in
# pyproject.toml [tool.pytest.ini_options]) is supported.
pytest>=7.0
pytest-asyncio>=1.0
